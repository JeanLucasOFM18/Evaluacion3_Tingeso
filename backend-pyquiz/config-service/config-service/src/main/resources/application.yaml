server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/JeanLucasOFM18/Evaluacion2_Tingeso
          search-paths: config-data
  application:
    name: config-service
